class Solution {
public:
    int lengthOfLastWord(string s) {
        long n=s.length(),l=0;
        for(int i=n-1 ;i>=0;i--)
        {
                if(s[i]==' ' && l!=0)
                break;
                if(s[i]!=' ' )
                l++;  
        } 
        return l; 
    }
};
