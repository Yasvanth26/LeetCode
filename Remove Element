class Solution {
public:
    int removeElement(vector<int>& nums, int val) {  int c=0;
        long n=nums.size(),j=0;
        for(int i=0;i<n;i++)
        {
            if(nums.at(i)!=val){
            nums[j]=nums[i];
            j++;

        }

        }
        return j;
    }
};  
